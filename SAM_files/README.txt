example_SAM_dupe_colors.png

this example sam file has not been sorted yet by chrom & bp start

same colored lines represent duplicates of each other

orange: clearly a duplicate due to same UMI, chrome, bp start

blue: when adjusting for the CIGAR string, all of them start at the same bp

green: clearly a duplicate due to same UMI, chrome, bp start

purple: when adjusting for the CIGAR string, all of them start at the same bp